const mensajeBienvenida = document.getElementById("mensaje-bienvenida");
mensajeBienvenida.textContent = `Bienvenido a OnlyFlans!`;
// Suponiendo que los datos del usuario se almacenan en un objeto llamado `usuario`
/* if (usuario) {
  if (usuario.nombre) {
    mensajeBienvenida.textContent = `Bienvenido ${usuario.nombre} a OnlyFlans!`;
  } else {
    // Mensaje de bienvenida genérico, incluso si usuario.nombre no está definido
    mensajeBienvenida.textContent = `Bienvenido a OnlyFlans!`;
  }
} else {
  mensajeBienvenida.textContent = `Bienvenido a OnlyFlans!`;
} */
